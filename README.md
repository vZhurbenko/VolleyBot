# Volleyball Poll Bot

Это продвинутый Telegram-бот для управления опросами о посещении волейбольных тренировок.

## Функциональность

- Создание регулярных опросов (повторяются каждую неделю)
- Удаление опросов по выбору
- Редактирование параметров шаблона через меню
- Показ результатов голосования
- Автоматическое создание опросов по расписанию
- Управление через inline-кнопки
- Защита от несанкционированного доступа (только администраторы)
- Поддержка нескольких администраторов
- Указание ID чата при создании опроса
- Автоматическая подстановка даты и времени тренировки

## Установка и запуск

1. Установите зависимости:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

2. Убедитесь, что ваш файл `data.json` содержит правильные настройки:
   - `secrets.auth_key` - токен вашего Telegram-бота
   - `admin.user_ids` - массив ID администраторов
   - Шаблон опроса в разделе `default_poll_template`

3. Получите ваш ID пользователя в Telegram:
   - Отправьте любое сообщение боту @userinfobot или @myidbot
   - Или добавьте этого бота к себе и используйте команду /getid
   - Также можно использовать бота @rawdata_bot, который покажет всю информацию о пользователе в формате JSON

4. Добавьте информацию об администраторе в файл `data.json`:
   ```json
   {
     "secrets": {
       "auth_key": "ваш_токен_бота"
     },
     "admin": {
       "user_ids": [ваш_реальный_ID_пользователя]
     },
     "default_poll_template": {
       "name": "Название опроса",
       "description": "Текст опроса {date} {time}",
       "training_day": "sunday",  // день недели тренировки
       "poll_day": "tuesday",    // день недели для автоматического создания опроса
       "training_time": "18:00", // время тренировки
       "options": [
         "Вариант 1",
         "Вариант 2",
         "Вариант 3"
       ],
       "enabled": true
     },
     "active_polls": {},
     "scheduled_polls": {}
   }
   ```

   Или используйте скрипт для автоматической настройки:
   ```bash
   ./set_admin.sh
   ```

   Также можно добавить новых администраторов через команду в боте:
   - Отправьте команду `/getid` в чат с ботом, чтобы получить ваш ID
   - В главном меню бота зайдите в "⚙️ Настройки" → "👥 Добавить администратора"
   - Введите ID пользователя, которого хотите добавить

5. Запустите бота:
   ```bash
   python3 bot.py
   ```

   Или используйте скрипт запуска:
   ```bash
   ./run_bot.sh
   ```

## Структура проекта

```
├── bot.py          # Основной файл бота
├── data.json       # Файл конфигурации
├── requirements.txt # Зависимости
├── README.md       # Документация (этот файл)
├── run_bot.sh      # Скрипт для запуска бота
├── stop_bot.sh     # Скрипт для остановки бота
├── set_admin.sh    # Скрипт для настройки администратора
└── venv/           # Виртуальное окружение (при локальном запуске)
```

## Конфигурация

Файл `data.json` содержит следующие разделы:

- `secrets` - содержит токен бота
- `admin` - информация об администраторах (массив user_ids)
- `default_poll_template` - шаблон опроса
- `active_polls` - активные опросы (управляется автоматически)
- `scheduled_polls` - запланированные опросы (управляется автоматически)

Шаблон опроса имеет следующие параметры:
- `name` - название шаблона для отображения
- `description` - описание опроса (можно использовать `{date}` для автоподстановки даты тренировки и `{time}` для времени)
- `training_day` - день недели тренировки (monday, tuesday, etc.)
- `poll_day` - день недели для автоматического создания опроса
- `training_time` - время тренировки (HH:MM)
- `options` - варианты ответов в опросе
- `enabled` - статус включен/выключен (по умолчанию true)
- `default_message_thread_id` - ID топика по умолчанию (null если не используется)
- `default_chat_id` - ID чата по умолчанию (для быстрого создания опросов)
- `default_topic_id` - ID топика по умолчанию (для быстрого создания опросов)

Расписание опроса имеет следующие параметры:
- `id` - уникальный идентификатор расписания
- `name` - название расписания
- `chat_id` - ID чата для отправки опроса
- `message_thread_id` - ID топика (если используется)
- `training_day` - день недели тренировки (monday, tuesday, etc.)
- `poll_day` - день недели для автоматического создания опроса
- `training_time` - время тренировки (HH:MM)
- `enabled` - статус включен/выключен (по умолчанию true)

## Команды бота

- `/start` - начать взаимодействие с ботом
- `/getid` - получить ваш ID пользователя в Telegram
- `/stop_bot` - остановить работу бота (только для администраторов)

## Управление ботом

- `./run_bot.sh` - запустить бота (в фоне не работает)
- `./start_bot.sh` - запустить бота в фоне с сохранением PID
- `./stop_bot.sh` - остановить бота (через сигнал)
- `./stop_bot_by_pid.sh` - остановить бота по сохраненному PID
- `/stop_bot` - команда для остановки бота через чат (только для администраторов)

## Inline-кнопки

- 📊 Создать опрос - создать опрос в указанный чат
- 🗑️ Удалить опрос - удалить выбранный активный опрос
- ✏️ Редактировать опрос - редактирование параметров шаблона
- 📋 Показать результаты - показать результаты выбранного опроса
- ⚙️ Настройки - дополнительные настройки бота
- 👥 Добавить администратора - добавить нового администратора

## Расписание

Бот автоматически создает опросы каждый вторник в 20:00 по серверному времени.
Это позволяет создавать опросы заранее на воскресную тренировку.
Расписание можно изменить в коде бота в функции `main()`.

## Требования

- Python 3.8+
- Библиотеки из requirements.txt

## Безопасность

- Не публикуйте токен бота в открытых репозиториях
- Убедитесь, что только доверенные лица знают ваш ID пользователя в Telegram
- Регулярно обновляйте зависимости бота

## Автор

@vzhurbenko with Qwen

## WIP (Work in Progress)

### Текущие проблемы:
- При создании опроса после выбора "Будем создавать опрос регулярно?" с кнопками "✅ Создать расписание" или "❌ Только один раз", процесс не завершается корректно. Необходимо разобраться с логикой обработки этих кнопок и завершением процесса создания опроса.